# Optional. Required only when openai_enabled is true.
OPENAI_API_KEY=

# Optional. If not set, the bot uses config.example.json or --config.
XAU_SNIPER_CONFIG=config.json

# Optional. Required only when pushover_enabled is true and tokens are not in config.
PUSHOVER_APP_TOKEN=
PUSHOVER_USER_KEY=
